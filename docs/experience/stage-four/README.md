# Light becomes useful electrical energy

[Local preview](http://127.0.0.1:4173/experience) · [Capture gallery](captures/index.html) · [Continuous forward/reverse recording](motion/journey-forward-reverse.webm) · [Complete vision register](vision-register.md)

Stage four extends the same selected panel into an attached glass/silicon section, absorption, contact collection, the module junction, a supported representative DC route and a generic inverter with first AC output. The white-gold incident guide ends at absorption. A separate blue energy-flow overlay carries the explanation into the circuit; the inset beside the inverter shows voltage versus time, not the path of a particle.

## Built and preserved

- Accepted source → Sun → flight → Earth → Great Britain → building → panel poses retain their original **12.6 useful viewport heights**. The extension adds **10.248**; named journey units now span **0–4.08**, at the unchanged 5.6 viewport heights per unit. Native scrolling and reversal reconstruct the same camera, aim and chapter state.
- One `panel-layout.ts` defines the 6×24 cell pattern, margins, three busbar positions and six finger positions. Both quality variants of the exterior texture register to the actual glass; the cell section uses the same layout. The duplicate exterior contact mesh was removed, eliminating the conflicting grid and abrupt detail switch.
- The hero anchor and normal remain authoritative. The protective-layer handoff at 2.38 uses the selected cell's registered local basis and 50× lateral scale, with intentionally exaggerated layer depth. A sectional aperture keeps the rest of the module attached. Dark silicon, translucent protection, restrained metallic contacts and a thin junction indication distinguish the materials.
- Absorption and extraction are deterministic phases. A localized response accompanies the disappearing incident guide; separate front/rear collection overlays then resolve toward contacts. No one-shot particles, accumulating events or claim that a photon becomes an electron. The existing illumination remains on; accessible HTML states the need for continuing light and a connected circuit.
- The camera returns around the module edge to its actual junction/lead coordinates, then follows representative paired cabling through roof containment, a covered crossing, parapet saddle and supported west-wall descent. The original campus is reused. The flow overlay respects opaque building depth; it cannot show a false cable through the wall.
- The original unbranded inverter has a rounded enclosure, mounting clearance, drip lip, gasket, back fins, controls and separate bottom cable glands. A steady DC reference and alternating AC trace use separate voltage/time axes. The physical AC cable remains a supported cable. No ratings, phase count, frequency, certification, savings, warranty or manufacturer-specific internals are asserted.
- Copy clears before the waveform appears. The final desktop composition places hardware/labels to the right of still-view copy; portrait frames keep the same elements below the statement. The application section follows the settled output frame.

The [science record](science.md), [cell provenance](cell-provenance.md) and [electrical provenance](electrical-provenance.md) identify exact primary sources, access dates and original asset creation. DOE pages were verified 8 September 2026; no source diagrams or external artwork were copied. This is a generic educational illustration with compressed distance/time and enlarged layers, not a surveyed installation, wiring design or manufacturer claim. Earlier NASA/Natural Earth records remain unchanged.

## Warnings and lifecycle

`PMREMGenerator.fromScene` now uses supported sigma **0.04**, verified against the pinned Three.js 0.185.1 sampling limits (20 samples at 256, 10 at 128), preserving the existing reflection environment. The renderer explicitly uses **PCFShadowMap**, the mode to which the deprecated setting already fell back. The shadow map is reused and refreshed on scene re-entry. No dependency upgrade or warning suppression was used; shader-readiness cancellation remains tested.

One renderer owns all scenes. Cell code is prefetched only after 2.08 with the site ready; electrical code after 2.83 with the cell ready. Earlier chapter prefetch remains unchanged. Every owner is cached once for reversal, with no future battery/business assets loaded. An eight-second import/decode/shader-readiness deadline holds a composed earlier view and then provides the HTML service alternative if needed. Cancellation precedes disposal. Context loss also removes projected HTML labels.

The eight reduced-motion stills are Sun, Earth, Britain, roof, panel, cell, DC route and inverter, without extended pinned travel. Ambient motion freezes for Pause, reduced motion, hidden pages and offscreen application details. Apply, Skip, focus handling, native keyboard access, no-JavaScript content and WebGL/import/context-loss fallbacks remain.

The service is unchanged: **G99 Form A1-2 preparation, submission and follow-up across Great Britain; £250 fee + £50 VAT = £300 total per application**. Installer/property-owner distinctions, eligibility/contact/legal/support links, all original fields and both consents remain. The fee is for the application service, not an installation; approval is not guaranteed. Original business files remain byte-identical to audited baseline `c61643f`. Local APIs block providers; checks use mocks and create no application or payment. The existing application page's external font stylesheet is mocked during the final recording, so no external font request is needed for that handoff.

## Verification and visual review

Type checking, the production build, **38 Node tests and 48 browser regressions pass**. Tests cover the exact shared layout/UV registration, 1,353 frozen earlier poses, photon termination and phase ordering, reverse transforms, trail attachment/prefix, cell/site handoffs, near-plane clearance, actual disposal/budgets, delays/failures/deadlines/late completions, shader cancellation, all eight stills, rapid keyboard Skip, copy/label separation and preserved business contracts.

The dense [geometry diagnostic](near-plane-clearance.json) checks 91,505 poses over all five framing/aspect combinations, using actual triangles and instances. Transparent material and full transition masks do not exempt collisions. The tested camera centre and near-plane rectangle have no solid intersections; the visible guide, DC reference front and settled cabinet/waveform fit the tested framings. Reproduce with `node scripts/verify-stage-four-clearance.mjs`.

Rendered forward/reverse frames were inspected at **1280×720, 1600×1000, 390×844, 740×900 and 1000×500**, including intermediate thresholds. Iteration corrected the dense panel contact pattern, optical trail reset, weak cell contrast, portrait waveform clipping, backward-looking DC camera, through-wall electrical overlay, inverter-label/headline overlap and persistent enlarged-text shading after copy cleared. Brief glass/metal occlusions conceal registered scale handoffs; the section and route remain illustrative.

The [gallery](captures/index.html) includes panel, glass, silicon, absorption, collection, contact handoff, return, DC, inverter and application frames. [Zoom/still observations](zoom-observations.json) distinguish 200% text sizing from a 640×360 CSS viewport at DPR2 that models 200% desktop zoom. The continuous recording scrolls from 2.18 to 4.08 in 35 seconds, returns in 30 seconds, then uses Skip and the real local `/apply.html` route. It is captured motion, not a compilation montage.

## Delivery and resource budgets

Stage-three baseline: **209,285 B JavaScript gzip / 1,844,085 B media and geographic JSON**. This build: **225,201 B JavaScript gzip / 1,844,085 B media**, including all variants. Increment: **15,916 B gzip JS and zero new media**. Existing ceilings remain 750,000 / 2,000,000 B. Cell code is 4,710 B gzip, electrical code 4,137 B, shared flow code 789 B, against enforced limits of 8,000 / 8,000 / 2,000 B. [Exact hashes and byte limits](../asset-sizes.json).

| New owned resources, including flow overlays | Desktop | Mobile | Enforced limit |
| --- | ---: | ---: | ---: |
| Cell geometry + instance buffers + collection route | 68,884 B | 57,940 B | 250,000 B |
| Cell triangles / base calls | 4,282 / 18 | 3,834 / 18 | 15,000 / 25 |
| Electrical geometry + instances + DC/AC overlays | 237,028 B | 163,924 B | 350,000 B |
| Electrical triangles / base calls | 10,266 / 14 | 7,682 / 14 | 20,000 / 25 |
| New decoded / mipmapped texture storage | 0 / 0 B | 0 / 0 B | 0 B |

The existing environment and shadow are shared, not duplicated for the cell or inverter. Retained Earth/site texture mipmaps plus the shared environment and shadow colour/depth targets remain approximately **102.33 MiB desktop / 25.58 MiB mobile** before other overhead. This includes **9 / 2.25 MiB** for environment colour/depth and **32 / 8 MiB** for shadow colour/depth. Added buffer storage is about **0.292 / 0.212 MiB**; these are buffer/texture storage estimates, not measured total GPU/process memory. Driver overhead, shader programs, other framebuffers, browser caches and temporary upload/capture copies are excluded. The adaptive DPR and total-pixel caps remain unchanged.

[Final frame measurements](performance-summary.json), with intervals in milliseconds:

| Viewport / requested DPR | Median, all seven holds | Sun p95 | Earth p95 | Panel p95 | Cell p95 | Contacts p95 | DC p95 | Inverter p95 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1600×1000 / 1 | 16.7 | 18.2 | 18.0 | 18.0 | 18.1 | 18.2 | 18.0 | 18.0 |
| 1280×720 / 1 | 16.7 | 18.0 | 18.2 | 17.9 | 18.0 | 18.3 | 18.0 | 18.3 |
| 390×844 / 3 | 16.7 | 17.6 | 17.6 | 17.6 | 17.6 | 17.6 | 17.6 | 17.6 |
| 740×900 / 1 | 33.3 | 34.2 | 34.1 | 34.1 | 34.1 | 33.7 | 34.0 | 33.9 |
| 1000×500 / 1 | 16.7 | 17.4 | 17.4 | 16.8 | 17.3 | 17.2 | 17.2 | 17.5 |

The 740×900 run has a steady ~30 fps cadence. A [fresh-browser repeat](cadence-repeat.json) measures the same **33.3 ms median on an empty page before loading, on the paused scene and on an empty page afterward**. It is a browser-run cadence ceiling in this measurement, not evidence of a new cell/inverter rendering bottleneck; its lower rate is retained in the report rather than discarded. Reproduce with `node scripts/check-stage-four-cadence.mjs`. The other four viewports have no sampled intervals above 33.4 ms in these settled holds. This does not establish performance on every device.

The final desktop inverter frame uses **53 draw calls / 110,188 triangles** at 1600×1000 and 1280×720; the short view uses 54 / 110,428. The cell absorption frame uses **20 / 4,452** on desktop and **19 / 3,992** at the phone viewport. Shadow refresh draws at chapter changes are additional transient work. After warming every view, all five runs retain **one renderer, 57 geometries and eight textures** with unchanged counts and no new requests through forward, reverse and repeated traversal. [Complete request, state and focus evidence](verification.json).

Actual loopback HTTP response bodies total **886,928 B initially / 2,337,192 B after all chapters** for desktop assets, and **808,926 / 1,595,548 B** for mobile assets. Total transferred bytes including reported response overhead are **889,028 / 2,341,692 B** desktop and **811,026 / 1,600,048 B** mobile. These are the local preview's uncompressed response bodies, distinct from the gzip build budget; `decodedBodySize` is not decoded image memory. Each complete run has 15 resource requests, with none added during the warmed reversals.

All five final runs record **zero console warnings, errors or verification problems**, including site first/repeat entry. Cold shader-warmup latency was not separately benchmarked; the frame samples begin only after readiness and a settling interval. Delayed readiness, cancellation and the eight-second failure deadline are separately tested behaviours, not warmup-performance claims.

Measurements use **Chrome for Testing 151.0.7922.34 on an Apple M4 MacBook Air with 16 GB, ANGLE Metal**. Each settled heavy view is sampled for four seconds. These are animation-frame intervals, not isolated GPU execution times or thermal tests. Phone-sized viewports are desktop emulation. Physical phones, Safari/Firefox, constrained networks and sustained thermal operation remain unmeasured.

## Recoverable handoff and remaining vision

Work is on **`experience/stage-four`** in `/Users/nick/Projects/Aesir Solar/experience-stage1`. The actual clean reviewed checkout **`6565351` / `experience-stage-three-delivered`**, later changes and the port-4173 server's working directory were checked before edits. That recoverable stage-three checkpoint remains. The final delivery is tagged **`experience-stage-four-delivered`**. `/Users/nick/Projects/Aesir Solar/site` remains untouched. Nothing was published or deployed, and the public homepage was not replaced.

Next: optional storage/business/grid context; considered lights/equipment/screens activation; a settled brand/service reveal and honest long-journey/final-connection payoff. The [vision register](vision-register.md) preserves independence, economics, longevity, sustainability, storage/resilience, monitoring, straightforward process, eligibility, proof, FAQs and support, separating confirmed copy from evidence still required. Optional audio/cursor polish and physical-device checks remain planned.

The independent [intake/payment/fulfilment blockers](../backend-blockers.md) remain a public-rollout requirement. This visual milestone does not repair them.

Reproduce with `npm run typecheck`, `npm run build`, `npm test`, `npm run test:browser`. Run `node scripts/capture-stage-four.mjs`, `node scripts/inspect-stage-four.mjs` and `node scripts/verify-stage-four.mjs` separately so recordings and timing measurements do not contend with other verification browsers.
