# Stage seven — cinematic finish and one local site

Local candidate: **http://127.0.0.1:4173/**. `/experience` remains compatible. Work is on `experience/stage-seven`, recovered from reviewed `dbb8197` / `experience-stage-six-delivered`. The original `/Users/nick/Projects/Aesir Solar/site` checkout and public deployment remain untouched. Recovery starts at the retained stage-six tag. The final local delivery is identified by tag `experience-stage-seven-delivered` on this branch.

Local visual review: **http://127.0.0.1:4177/stage-seven/review.html**. This separate loopback-only evidence server serves the saved review artifacts, not a second product site; it also sends noindex headers. The gallery file remains usable locally if that temporary server is stopped.

## What changed

The film retains its 23 chapter meanings, 12 still views and 6.08 units / 34.048 useful viewport heights. This is a finish pass with intentional, documented camera/copy changes, not a shorter opening or another chapter.

- Sun detail is filtered against its projected footprint; surface, prominence, corona and guide output have a controlled radiance/colour treatment. There is no new global bloom or postprocessing package.
- The two geographic rebases use different high-atmosphere strata and low cloud banks, with directional parallax and tightly bounded complete conceal. The original geographic target and coordinate convention remain.
- Recessed module backing removes the real coplanar cause of serrated panel edges. Connected glass panes retain one selected cell and avoid internal overlapping faces. Focused shadow coverage and selective casters improve the close view at the same map resolution.
- The camera moves closer to absorption, particularly in portrait. Copy leads the event and clears for it. The incident guide ends; electrical collection remains a different cue. The contact return uses actual module structure instead of a full black veil. A bounded early route fade and soft source closure keep the acquired DC route legible end-on. The last shader correction holds only the existing terminal cap at a steady 0.90 signal, below the earlier 0.95 moving-bar peak; the source geometry, tube and camera are unchanged.
- Entry framing passes beside the actual riser through the existing facade opening. The machinery now advances cartons and dwells for the packing head; covered transfers conceal the deterministic loop. Local contact artwork follows the actual feet and cartons. Generic inverter/converter/battery construction has selective physical detail.
- Grid import is established at service-connection scale before the wider view. The final same-campus scene has restrained practical light on the actual pavement, warm office activity, crisp Aesir identity and both inline/header application actions.
- Pause freezes the rendered composition and copy as well as ambient time, including delayed owner readiness and the cell/contact join. Resizing replaces only the selected Sun geometry; existing owners retain their initial texture tier.

The [shot sheet](shot-sheet.md) records every boundary and scoped pose-fixture changes. The [review gallery](review.html) pairs the recovered baseline and candidate, provides recordings, and links the complete page captures. The source baseline has been rebuilt separately under `/private/tmp/aesir-stage-seven-baseline`; settled comparison captures wait for the same canvas opacity. Earlier trial captures remain iteration evidence, not final results.

## One coherent candidate site

The film is the homepage at `/`, `/index`, `/index.html`, `/experience` and `/experience.html`. Application, contact, policies and the explicit simulator-retirement/planning destination share its dark/gold identity without importing 3D. FAQ compatibility leads into the merged native FAQ section. Existing fragments are real focusable HTML anchors, including `#top`, `#main`, `#gate`, `#check`, `#work`, `#price`, `#apply` and `#realroof`.

The new suitability guide asks for documented AC information, keeps unknown/out-of-scope cases on a contact path and does not certify a design or create a paid verdict. Current primary ENA/operator wording and limits are in the [claim-ledger addition](claim-ledger-addition.md). Contact uses the supplied `hello@aesirsolar.co.uk` and practical applicant-support prompts. No phone, address, hours, account portal, rating, warranty, saving or customer claim was invented.

All 19 named application controls, required/default/validation attributes, both consents, prefill/cancellation context, £300 fee and existing provider selection remain. The form cannot submit a personal-data GET before its unchanged controller loads or without JavaScript. A controller failure leaves an email alternative. The return page now states that payment/application status is unverified; URL references are text only. No authoritative verification or transaction repair is implied.

The full policies survive, with narrow corrections to inaccurate implementation descriptions and the qualified refund/consent wording. Contracting-entity information and proof of policy operations still require owner/legal release review. The dated Premier Composites example remains 24 August 2026, 206.41 kWh, supplied Tigo transcription, distinct from the 879-module illustrative campus and not independently certified or live.

See the [route/content and source-ownership manifest](route-content-manifest.md), [migration notes](site-migration-notes.md) and [independent route audit](final-route-review.md). `build.py` generates supporting pages from `templates/site/`; assembly always runs it, stages an explicit output allowlist and replaces only the generated candidate directory. Repeated-build tests prove deleted marketing output cannot linger. All original API/helper/provider configuration contracts remain protected.

## Verification and performance

Final machine-readable records: [checks](review-checks.json), [performance](performance.json), [final motion](final-motion-observations.json), [page captures](site-captures/observations.json), [film captures](after/observations.json), [resource traversal](final-resources.json), and [final actual-geometry/frustum clearance](final-clearance.json). The final implementation passed type checking, build, **78 Node tests and 112 browser checks**. The check record pins the source and all 52 delivered files by hash. All five normal film captures, complete forward/reverse recordings and benchmark views reported zero browser errors or warnings. The intentionally failed-import test records its expected errors separately.

The completed recordings under `final-motion/` cover the full forward/reverse journey in all five viewport shapes, a slow contact/DC pass and a boundary hold. Native-scroll rounding put three exact-3.08 holds just before the scene change; the [supplemental terminal recordings](terminal-verified/observations.json) explicitly verify the site scene, ready electrical owner and visible DC route at 3.085 in every viewport. Each full recording passed Pause, Skip/focus and offscreen-suspension checks. The [earlier motion record](motion-observations.json) and `motion/` retain five full journeys plus nine slow joins and separate absorption/DC passes without copy on laptop and mobile. Those remain scoped evidence for the earlier revision, not recordings of the terminal-cap correction.

The predeclared incremental detail envelope is 40,000 buffer bytes / 10 base draws / 7,000 triangles. Final authored detail uses **34,734 bytes, 10 draws, 5,925 desktop / 5,669 mobile triangles**, with no new texture, render target, actual light or downloaded media. Existing delivery ceilings remain 750,000 B JS gzip / 2,000,000 B media. [Budget accounting](budgets.md) distinguishes transfer, buffers, decoded textures, targets and unknown process overhead.

Final transfer is **254,502 B experience JavaScript gzip**, **262,670 B across all served browser JavaScript**, **7,800 B experience CSS gzip**, and **1,844,085 B media/geographic JSON**, with no media increase. Across the five viewport shapes, all eight sampled heavy scenes had a **16.7 ms median / 17.3–17.5 ms p95 frame interval**. Opening readiness was **149–165 ms**; new-owner first readiness ranged **22.6–114.2 ms** on loopback. These are rAF cadence and local readiness observations, not GPU execution times or internet/phone predictions. Three repeated traversals retained **79 geometries / 8 textures / 22 requests**; editorial reading produced no render-frame samples. The [measured report](performance-summary.md) records per-view values and cache limitations.

The earlier dense business check tested 1,620,405 pose/frustum pairs. The bounded final-clearance record retests 81,405 pairs against its recorded source hashes, including the source closure and pavement receivers. Both find zero collisions in their tested geometry. The original macro/glass checks also inspect finite near-volume clearance, selected-cell registration and the connected aperture. Scope and precision are recorded in the reports; they are not proof about every possible physical device.

Measured host: Apple M4 MacBook Air, 16 GB, Mac16,13; Chrome for Testing 151.0.7922.34 using ANGLE Metal. The five requested viewport shapes are 1280×720, 1600×1000, 390×844, 740×900 and 1000×500. Phone-sized views are desktop emulation. Physical phones, Firefox and sustained thermal validation remain unavailable. Enlarged-text and 640×360 DPR2 zoom-layout tests are distinguished from operating a browser's native zoom menu.

Safari **26.6.2** received a limited native desktop smoke review through CUA: Sun approach, selected cell, working bay, Pause, Skip/focus at application details, the application form and Back to the paused offer. [Browser availability and scope](browser-availability.json) and the [native captures](safari-native/) record that review. The [first](safari-native/paused-1.png) and [second paused capture](safari-native/paused-2.png) have identical central scene pixels; only the native pointer/click highlight beside Pause differs. Safari WebDriver could not start because remote automation is disabled; no setting was changed. This is not a full Safari regression, console audit or performance measurement. Those checks, along with physical-device and broader-engine coverage, remain open.

The preview is loopback-only, sends `X-Robots-Tag: noindex, nofollow`, serves disallow-all robots and returns isolated 503 JSON for API routes. Regression tests use local fixtures/mocks; no real application, payment, telemetry or message was sent.

## What still prevents public cutover

The page and film do not prove that a paid application becomes work for Aesir. The separate [backend blocker report](../backend-blockers.md) remains applicable:

1. This should save the complete installation details and both consent records under a recoverable application reference. The current browser copy and provider payload do not prove a durable complete intake.
2. This should match that application to the correct verified £300 GBP payment. A return URL or displayed reference is not authoritative evidence; the new page now says so instead of claiming success.
3. This should create one actionable Aesir work item, with retries that do not duplicate it. That end-to-end fulfilment result remains unproven.

Server suitability enforcement, legacy telemetry measurement freshness, verified legal identity/policy operations, final owner art-direction acceptance and broader physical-device/browser checks remain open. Audio/cursor polish is optional. Product-specific economics, warranties, backup arrangements and proof need their own evidence; no placeholder claims have been published. The [complete vision register](vision-register.md) keeps every commercial topic and remaining gate explicit.

## Reproduce locally

Use the pinned Node/dependencies. Run `npm run typecheck`, `npm run build`, `npm test`, `npm run test:browser`. The preview runs with `npm run preview`. `python3 build.py --check` verifies generated source parity. Capture/review scripts are `capture-stage-seven.mjs`, `capture-stage-seven-site.mjs`, `record-stage-seven.mjs`, `record-stage-seven-terminal.mjs`, `verify-stage-seven.mjs` and `check-stage-seven-clearance.mjs`. Run GPU timing alone, separately from recordings or other browser workloads.

### Preference-switch regression

A simultaneous Chromium media-preference/viewport update sometimes changed the live `matches` value without notifying the original listener. The [handler trace](preference-handler-trace.log) captures the missing callback. The animation loop now reconciles the preference before suspending, and layout/visibility changes also reconcile it. This does not introduce polling or continuous paused rendering. The new regression deliberately drops the notification and checks static controls, frozen time and restoration on resize; the original doubled-text test remains intact. [Ten repeated checks](preference-regression.log) passed before the final full run.

### Post-test form review

The saved 200%-text short-screen application exposed narrow selected supply/date fields. The supporting form now collapses both grids at narrower widths and when enlarged controls are detected. Its heading is also shorter so punctuation does not become an isolated line. A follow-up [reproduction](form-gap-reproduction.log) showed that the inherited percentage row gap let the stacked order summary extend about 161px beyond the main content into the footer. The single-column application grid now uses a fixed **40px gap** in both responsive and enlarged-form modes.

The original 19 controls, consents and payment controller are unchanged. The focused regression measures the selected supply text against its available width at 1000px and 1600px with doubled text, and checks that the order summary stays inside main and above the footer. No additional network or rendering loop is introduced. Final pass counts belong to the [completed check record](review-checks.json).

### Paused preference round trip

A final independent state review found that returning from still views could retain the Resume label while releasing the scene freeze. The normal-mode setup now re-freezes the actual rendered composition when Pause is active, before delayed readiness can change it. A focused test gates the cell owner across Pause → reduced motion → normal motion, releases it and checks composition/copy/time before keyboard Resume. This changes the mode-switch contract only; the recorded ordinary forward/reverse choreography is unchanged.
