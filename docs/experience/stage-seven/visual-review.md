# Stage seven visual review

The comparison gallery uses settled captures from the recovered `dbb8197` build and the candidate. Both wait past the canvas fade; comparing an early fading canvas against a settled one would falsely attribute exposure changes to the shader. Ambient phases differ. The preserved recordings and sampled progress traces complement the stills; screenshots alone do not establish motion quality.

## What the reviewed frames show

- The Sun retains its enormous right-hand composition. The candidate has a stronger pale-gold luminous hierarchy, fewer unstable fine surface frequencies and visible attached prominence structure. It remains a cinematic scientific illustration.
- The high-atmosphere Earth/Britain passage and lower cloud/site passage have different structure and directional layering. The actual coordinate rebase still needs a short complete conceal; it is bounded, deterministic and reversible. Britain remains the target, and the campus remains illustrative.
- The hero frame now has clean metal edges. The earlier serration came from coplanar backing/frame surfaces, rather than insufficient global resolution. Recessing the backing removes that overlap without blurring the panel.
- In portrait, the selected-cell absorption is large enough to see in the lower-middle image, with copy clearing before the event. The optical guide ends and the collected energy has a separate cyan contact route. The enlarged stack and continuing-illumination/connected-circuit qualification remain accessible.
- The contact return uses the same module as its structural occluder. The electrical source remains visible when the initial tube is viewed almost end-on; its soft closure and short-prefix fade do not create a second optical guide.
- The workspace entry no longer centres the service riser. Cartons advance with the conveyor and dwell beneath the packing head, with recirculation behind covered transfers. Contact receivers follow actual machinery feet and cartons.
- The grid import is shown close enough to follow before widening. The final campus keeps roof, facade, warm office and finite practical pavement pools distinct. The mark and both application actions retain contrast.

The countryside, hardware and cutaway remain deliberately simplified original illustration. Far roof lines at the retained pixel caps are not perfectly equivalent to offline architectural rendering. Final owner art-direction acceptance and physical-device/browser/thermal validation remain release work; these limits do not substitute for the implemented local refinement.

## Supporting page review

The offer remains the first substantive section after the film. Native anchors, direct email support, the conservative AC-information guide, retirement/planning destination, truthful payment return and complete policies use the same palette and typography. Supporting routes load no 3D code or assets.

The final enlarged-text review exposed narrow paired application controls. Both form grids now collapse at the relevant width or enlarged control text, and the shorter heading avoids orphan punctuation. The form controller, all 19 named controls and both consents retain their contracts. Ordinary footer screenshots are included separately from Chromium's long-page compositing captures.

## Evidence boundaries

Normal film captures and full recordings cover all five requested viewport shapes. Laptop and portrait recordings include slow forward/reverse passes through all nine priority joins and absorption/DC passes with chapter copy hidden. Access captures separately cover doubled text, a zoom-equivalent layout, no JavaScript and an intentionally failed renderer import. The failure case's expected console errors are recorded as such, not counted as a clean normal load.

The preference reconciliation and paused-mode round-trip fixes affect mode switching, not the recorded ordinary journey paths, materials or geometry. Final regression and benchmark records identify the final source/build. No phone-sized capture is represented as a physical-phone measurement.

### Final enlarged-grid correction

The first reflow fix exposed a second issue during visual inspection: the inherited percentage grid gap became a vertical percentage after the layout changed to one column. That gap was excluded from intrinsic height and let the order summary overrun the footer. Single-column application layouts now use a fixed 40px gap. The regression checks actual order-summary, main and footer bounds at both enlarged widths, not merely horizontal overflow. The saved failing frame is in `site-review-before/`; final recaptures follow the corrected build. The ordinary mobile footer viewport is clean, so the earlier repeated hero strip in the very long homepage capture is a screenshot-compositing artifact.

### Terminal steadiness after recorded review

The independent motion review examined 216 frames and 1,306 slow-pass trace states across nine joins, including copy-hidden passes. It found no sampled clipping or reversal discontinuity, but the DC terminal could dim between the directional marks after the structural occlusion. Its existing soft cap now has a steady 0.90 signal, below the previous 0.95 peak; the moving tube still uses its original modulation. The before-exit module occlusion remains motivated and bounded. The `terminal-final/` laptop/portrait frames verify the clearer reacquisition; `final-motion/` adds updated full traversal and a slow return in every viewport. Three exact-boundary holds landed just before exit through native-scroll rounding; `terminal-verified/` supplies explicit post-exit state assertions, 5.2-second holds and both slow directions for all five viewports. No new source object, geometry, bloom, light or asset is introduced.
