# Existing application/payment blockers — verified 8 September 2026

> **9 September checkout investigation:** production still sends applicants from Vercel to the old Fasthosts WordPress cart because Vercel has no Tyl/Stripe configuration. The real domain is not assigned to Vercel. The local `experience/checkout-consolidation` branch removes the fallback and prepares truthful failure handling; it is not deployed and does not repair durable intake or payment verification. The current request explicitly authorizes completing the Tyl/domain migration, superseding the older visual-only provider freeze below. Authenticated WordPress/Fasthosts/Tyl inspection is pending. See the [current evidence, local checks and remaining work](../operations/checkout-migration/README.md).

The visual preview can help visitors understand why to apply. The existing application flow still cannot prove that a complete application, a verified £300 payment and one actionable Aesir work item belong together. The preview does not repair that chain, and public homepage cutover remains blocked until it is proven.

| This should look like… | What happens now | What is broken between the two |
| --- | --- | --- |
| An installer pays once, and Aesir can open all installation answers and consent evidence against one application reference. | Answers are written to the browser on submit, Tyl carries only a subset, and its notification prints a smaller subset to logs. | No durable application intake, shared application identity or fulfilment record. |
| A customer sees “Payment received” only when the server has confirmed the correct application’s £300 GBP transaction. | Direct access to `success.html` says payment and application received. A locally signed dummy Tyl return for a different order, £1-equivalent amount and USD also redirects there. | The page has no authoritative status lookup; Tyl return has no stored expected order/amount/currency binding. This is not signature forgery. |
| An out-of-scope system goes to a suitability discussion before buying Form A1-2. | The simulator can label a system “a full G99 application” and still send it to the £250 + VAT Form A1-2 page; the homepage’s out-of-scope verdict leaves its application CTA active. | Eligibility findings do not govern the paid product route or server acceptance. |

## Source-verified launch blockers

All references below are relative to `/Users/nick/Projects/Aesir Solar/site`, verified against clean commit `c61643f`.

| Blocker | Exact evidence | Required outcome |
| --- | --- | --- |
| No complete durable intake or restore | `app.js:354` collects form data; `app.js:364` writes `aesir.application`, with no read anywhere in the source. `app.js:34` declares unused `FORM_ENDPOINT`. | Save and validate every field and consent before payment; restore/recover safely with a stable reference. |
| Payment requests accept incomplete applications | `api/checkout.js:63` and `api/tyl-checkout.js:43` validate email alone. `app.js:324` checks nonempty fields/checkboxes, not number validity despite `apply.html:63` using `novalidate`. | Enforce required fields, numeric semantics, both consents and the reviewed product suitability decision server-side. |
| Tyl loses required working context | `api/tyl-checkout.js:75` sends billing identity; `api/tyl-checkout.js:90` custom fields omit accreditation, type-test reference, storage, target date, notes, consent evidence and submission time. `api/tyl-notify.js:49` logs only selected fields. | Payment metadata references a complete stored application; it must not be the sole application copy. |
| No verified idempotent fulfilment | `api/tyl-notify.js:44` computes verification, but `api/tyl-notify.js:62` always acknowledges 200 after logging, including invalid signatures. `api/checkout.js:125` creates sessions but no Stripe webhook or durable fulfilment implementation exists among the five API handlers. | Authenticate provider events, bind to expected application/order/amount/currency/status, deduplicate and persist one actionable work item with retry safety. Confirm Tyl’s required signature format using official vectors and provider testing. |
| Unverified confirmation/receipt claims | `success.html:49` unconditionally claims payment; `success.html:54` claims Stripe receipt and received details. The same generated copy lives in `build.py:501`. `api/tyl-return.js:32` hashes the supplied amount/currency; `api/tyl-return.js:58` redirects using supplied order ID without comparing query order or stored expected state. | Provider-neutral status from durable state. Direct, stale, unknown and pending visits must not claim payment or receipt. Update generator and generated page together in that later repair. |
| Retry/cancellation can lose context or create another payment attempt | New Tyl ID at `api/tyl-checkout.js:51`; Stripe fetch lacks idempotency key at `api/checkout.js:126`; no request timeout. Stripe cancellation returns `?cancelled=1` at `api/checkout.js:97`, but `app.js:463` reads only `payment`. | One stable application identity with explicit payment-attempt states, timeout recovery and provider-neutral cancellation/pending/retry handling. |
| WooCommerce fallback has no application handoff | `app.js:17` defines product/cart URLs; `app.js:21` uses a browser timestamp to choose one; `app.js:370` navigates without application fields/reference. | Prove equivalent identity/intake handoff and safe quantity/idempotency before treating fallback as reliable. Preserve it during this visual milestone; external WooCommerce settings are unverified. |
| Out-of-scope checkout routing | `sim.js:121` treats panel DC kWp as three-phase inverter capacity; `sim.js:262` chooses full G99 wording while `sim.js:265` still links to `apply.html`. `app.js:199` supplies an above-60A verdict; `index.html:271` stays an application CTA. Prefill at `app.js:281` omits type-test/verdict and is not expired. | One reviewed suitability decision using actual registered AC capacity and the required checks; route unsuitable/uncertain cases to contact, preserve complete context and enforce the decision on the server. |
| Unsafe no-JavaScript form behaviour | `apply.html:63` has neither action nor method and disables native validation. | A deliberate fallback that cannot submit personal installation details into a GET URL or imply they were saved. |
| Callback and payment cache boundaries | `api/checkout.js:69` and `api/tyl-checkout.js:48` use request Origin/Host. `vercel.json:50` applies public caching across `/api/*`; only Tyl checkout explicitly sets `no-store` (`api/tyl-checkout.js:101`). | Trusted configured callback origin; explicit noncacheable payment/status responses, public caching limited to telemetry. |
| Stale figures presented as current evidence | `index.html:303` says “Today”; `app.js:450` inserts fixed historic fallback values without changing that label. | Distinct loading/current zero/dated historical/unavailable states with source timestamps; no invented live evidence. |

## Next bounded repair (outside this visual milestone)

Implement and prove one end-to-end application transaction: validate and durably save the complete existing field contract with policy-version/consent evidence and a stable application ID; initiate the existing server-priced £300 payment against that record; verify the provider notification against expected order, amount, currency and final status; atomically record payment and one fulfilment work item; return truthful status and safe recovery through the existing routes. Include a deliberate no-JavaScript form fallback, callback origin/cache restrictions and suitability enforcement because they affect this same transaction. Preserve provider choices, URL forms and field meanings. The telemetry freshness correction is a separate bounded content/data repair.

Keep `TYL_STORE_ID`, `TYL_SHARED_SECRET`, `TYL_GATEWAY_URL`, `STRIPE_SECRET_KEY`, optional `STRIPE_TAX_RATE_ID`, optional `TIGO_PUBLIC_URL` and `TIGO_SYSTEM_ID`. Retain `/api/tyl-checkout`, `/api/tyl-return`, `/api/tyl-notify`, `/api/checkout`, `/api/tigo` and all existing page/anchor destinations. No new provider, production credential change or invented backend has been introduced.

## Observed offline checks

On Node `v24.15.0`, imported the four payment handlers directly in a one-process harness. Set literal dummy environment values, fully replaced `globalThis.fetch` with a Stripe-response stub, and used fake request/response objects. No HTTP server, provider request, cart, order, real application, email or credential read occurred.

Reproduced email-only acceptance in both initialisers; Tyl’s missing working fields; untrusted Origin in both callback sets; invalid Tyl notification acknowledged `200 OK` with `verified:false`; and a valid dummy-signed return for an unexpected order, amount `1.00` and currency `840` reaching `/success`. Also verified existing fixed gross price/currency and single Stripe quantity; a client-supplied Tyl price did not change its total; both initialisers rejected invalid email; checkout/notification GET returned 405 and return GET redirected to `/apply`. These observations confirm code behaviour, not provider interoperability or actual customer harm.

## Meaningful offline regression suite to retain

1. **Preservation:** compare the baseline inventory/hashes of all nine existing pages, five APIs, helpers, existing frontend scripts, generator and deployment file; check each original extensionless/`.html` route and homepage anchor in the proposed build output; confirm no SPA rule captures `/api/*`.
2. **Existing payment contracts:** fake requests, dummy credentials and a fetch stub that throws for every unexpected call. Assert £300/GBP, quantity one, expected field naming, method handling, invalid-email rejection, missing-config responses and supported success/pending/declined/unverified redirect shapes. Cover optional Stripe tax-rate encoding without making claims about a live tax-rate configuration.
3. **Known-defect reproductions:** keep the email-only, wrong-bound-order, invalid-notification-ack and static-success probes visibly labelled as existing defects. A passing reproduction is not a launch acceptance test. Do not bless those behaviours as requirements.
4. **Repair acceptance (future):** incomplete/unsuitable requests create neither payment nor fulfilment; all application fields survive reopen and provider fallback; duplicate/out-of-order callbacks produce one paid transition and one work item; wrong amount/currency/order/invalid signature produce none; direct success is truthful; closed-tab payment can fulfil; failed intake creates no payment; cancellation and timeout recover the same application safely.
5. **Browser isolation:** intercept all payment, telemetry and external cart requests before loading or submitting dummy forms; block every unmatched external request. Assert preview CTA works before WebGL readiness, while paused and after failure, and that the preview bundle is absent from application/legal/contact pages. Record real-device and provider certification as untested until actually exercised.
